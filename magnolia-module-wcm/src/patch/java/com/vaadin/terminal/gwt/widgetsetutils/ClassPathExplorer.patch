Index: ClassPathExplorer.java
===================================================================
--- ClassPathExplorer.java	(revision 37132)
+++ ClassPathExplorer.java	(working copy)
@@ -521,17 +521,15 @@
      */
     private static void tryToAdd(final String fullclassName,
             Collection<Class<? extends Paintable>> paintables) {
+
+        PrintStream out = System.out;
+        PrintStream err = System.err;
         try {
-            PrintStream out = System.out;
-            PrintStream err = System.err;
             System.setErr(devnull);
             System.setOut(devnull);
 
             Class<?> c = Class.forName(fullclassName);
 
-            System.setErr(err);
-            System.setOut(out);
-
             if (c.getAnnotation(ClientWidget.class) != null) {
                 paintables.add((Class<? extends Paintable>) c);
                 // System.out.println("Found paintable " + fullclassName);
@@ -546,6 +544,10 @@
         } catch (Exception e) {
             e.printStackTrace();
         }
+        finally{
+            System.setErr(err);
+            System.setOut(out);
+        }
     }
 
     /**


